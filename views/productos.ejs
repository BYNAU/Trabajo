<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jua&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">

    <title>Document</title>
</head>
<body>
    <section>

        <%- include('partials/header2.ejs') %>
        
        <div id="cont">
            <% if (listaFavoritos.length > 0){ %>
                <div  style="display: flex; flex-direction: row;">
                    <% for (let i = 0 ; i < listaFavoritos.length ; i++){ %>
                        <% for (let j = 0 ; j < listaFavoritos[i].favoritos.length ; j++){  %>
                            <% if (listaFavoritos[i].favoritos[j] == nombre){ %>
                                <div>
                                    <h1><%= listaFavoritos[i].nombre %></h1>
                                    <img style="width: 300px;" src="<%= listaFavoritos[i].foto %>" alt="">
                                    <div style="display: flex; justify-content: center; align-items: center;">
                                        <form action="/eliminar" method="post" style="display: flex; justify-content: center; align-items: center;">
                                            <input name="nombre" type="hidden" value=<%= listaFavoritos[i].nombre %>>
                                            <button type="submit" style="width: 100px;">Eliminar</button>
                                            
                                        </form>
                                        <form action="/modificar" method="get" style="display: flex; justify-content: center; align-items: center;">
                                            <input name="nombre" type="hidden" value=<%= listaFavoritos[i].nombre %>>
                                            <button type="submit" style="width: 100px;">Modificar</button>
                                            
                                        </form>
                                        
                                    </div>
                                        
                                </div>
                                
                            <% } %>
                        <% } %>
                        
                    <% } %>
                </div>
                
                
                <a href="/productos/<%=nombre%>"><button>Añadir</button></a>



            <% } else { %>
                <h1>Este usuario no tiene ningun producto en favoritos</h1>
                <a href="/productos/<%=nombre%>"><button>Añadir</button></a>
                
            <% } %>
        </div>
        <%- include('partials/footer.ejs')%>
    </section>
</body>
</html>
<%- include('partials/estilos.ejs')%>
